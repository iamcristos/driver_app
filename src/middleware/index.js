import driver from './driver';
import association from './association';
import contribution from './contribution';

const middleware = { driver, association, contribution };

export default middleware;
